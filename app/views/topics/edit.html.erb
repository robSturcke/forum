<div class="container">
  <h1>Edit Topic</h1>
   <div class="row">
     <div class="col-md-4">
       <p>Guidelines for topics:</p>
       <ul>
         <li>Cover broad topics such as Beer Recipes, Beer Techniques etc.</li>
         <li>Try to cover similar sub-topics in Labels</li>
       </ul>
     </div>
     <div class="col-md-8">
       <%= form_for @topic do |f| %>
         <div class="form-group">
           <%= f.label :name %>
           <%= f.text_field :name, class: 'form-control', placeholder: "Enter topic name" %>
         </div>
         <div class="form-group">
           <%= f.label :description %>
           <%= f.text_area :description, rows: 8, class: 'form-control', placeholder: "Enter topic description" %>
         </div>
         <div class="form-group">
           <%= render partial: 'labels/form', locals: { f: f, labels: @topic.labels } %>
         </div>
         <%= f.submit "Save", class: 'btn btn-success' %>
       <% end %>
     </div>
   </div>
</div>
